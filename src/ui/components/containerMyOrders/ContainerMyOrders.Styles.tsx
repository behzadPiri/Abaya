import {StyleSheet} from 'react-native';

const createStyles = () =>
  StyleSheet.create({
    container: {
      flexGrow: 1,
      width: '100%',
      paddingHorizontal: 16,
      paddingBottom: 120,
    },
  });

export default createStyles;
